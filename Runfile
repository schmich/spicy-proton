doc 'Build binary corpus from *.yaml files.'
run :binary do
  system 'ruby -Ilib -rbinary -e "Spicy::BinaryCorpus.generate"'
end

doc 'Build Ruby gem.'
run :build do
  run :binary
  system 'gem build spicy-proton.gemspec'
end

auto
doc 'Run tests.'
run :test do
  system 'ruby -Ilib test.rb'
end
